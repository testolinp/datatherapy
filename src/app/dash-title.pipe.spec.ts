import { DashTitlePipe } from './dash-title.pipe';

describe('DashTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new DashTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
